<UserControl x:Class="Dungeoneer.View.ValueButton"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d">
	<Grid Background="Transparent" Loaded="GridLoaded">
		<Border x:Name="border" Margin="2" BorderBrush="White" Background="Transparent" BorderThickness="1,1,1,1" CornerRadius="4,4,4,4">
			<Button x:Name="button" BorderBrush="{x:Null}" Background="Transparent">
				<StackPanel x:Name="stackPanel">
					<Label x:Name="titleLabel" FontSize="14" HorizontalAlignment="Center" VerticalAlignment="Center"
								 HorizontalContentAlignment="Center" VerticalContentAlignment="Center"/>
					<Label x:Name="valueLabel" FontSize="16" HorizontalAlignment="Center" VerticalAlignment="Center"
								 HorizontalContentAlignment="Center" VerticalContentAlignment="Center"/>
				</StackPanel>
			</Button>
		</Border>
	</Grid>
</UserControl>
