<UserControl x:Class="Dungeoneer.View.CreatureControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:view="clr-namespace:Dungeoneer.View"
						 xmlns:model="clr-namespace:Dungeoneer.Model"
             mc:Ignorable="d" 
             d:DesignHeight="76" d:DesignWidth="596">
	<Grid>
		<Border Margin="2" BorderBrush="White" BorderThickness="1,1,1,1" CornerRadius="4">
			<DockPanel>
				<TreeView DockPanel.Dock="Bottom" Background="Transparent" ItemsSource="{Binding AttackSets}">
					<TreeView.Resources>
						<HierarchicalDataTemplate DataType="{x:Type model:AttackSet}" ItemsSource="{Binding Attacks}">
							<StackPanel Orientation="Horizontal">
								<TextBlock Text="{Binding Name}"/>
								<TextBlock Text=" ["/>
								<TextBlock Text="{Binding Attacks.Count}"/>
								<TextBlock Text="]"/>
								<!-- How about a button to roll an attack and display the results? -->
							</StackPanel>
						</HierarchicalDataTemplate>
						<HierarchicalDataTemplate DataType="{x:Type model:Attack}">
							<TextBlock Text="{Binding AsString}"/>
						</HierarchicalDataTemplate>
					</TreeView.Resources>
					<TreeView.ItemContainerStyle>
						<Style>
							<Setter Property="TreeViewItem.IsExpanded" Value="True"/>
						</Style>
					</TreeView.ItemContainerStyle>
					<TreeView.Style>
						<Style TargetType="{x:Type TreeView}">
							<Setter Property="Visibility" Value="Collapsed"/>
							<Style.Triggers>
								<DataTrigger Binding="{Binding IsChecked, ElementName=AttacksToggleButton}" Value="True">
									<Setter Property="Visibility" Value="Visible"/>
								</DataTrigger>
							</Style.Triggers>
						</Style>
					</TreeView.Style>
				</TreeView>
				<StackPanel DockPanel.Dock="Left" Orientation="Horizontal">
					<Label Name="NameLabel" Margin="10" Content="{Binding DisplayName}" FontSize="24" FontWeight="Bold"/>
					<StackPanel Orientation="Vertical">
						<ToggleButton x:Name="AttacksToggleButton" Content="Attacks" Margin="5" Height="25"/>
					</StackPanel>
					<view:ValueDisplay Title="AC" ValueBinding="{Binding ArmorClass}"/>
					<view:ValueButton Title="HP"
														ValueBinding="{Binding HitPoints}"
														CommandBinding="{Binding DoDamage}"
														PanelOrientation="Vertical"/>
				</StackPanel>
				<StackPanel DockPanel.Dock="Right" Orientation="Vertical" Width="90" HorizontalAlignment="Right" VerticalAlignment="Center" Margin="4,0">
					<Button Content="Effects" Command="{Binding ShowEffectsWindow}" Margin="5"/>
				</StackPanel>
			</DockPanel>
			<Border.Style>
				<Style TargetType="{x:Type Border}">
					<Setter Property="Background" Value="LightGray"/>
					<Style.Triggers>
						<DataTrigger Binding="{Binding Active}" Value="False">
							<Setter Property="Background" Value="Gray"/>
						</DataTrigger>
					</Style.Triggers>
				</Style>
			</Border.Style>
		</Border>
	</Grid>
</UserControl>
